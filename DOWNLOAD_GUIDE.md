# shift_bot 完全版 - ダウンロード手順

## 📦 含まれているもの

この完全版には、以下のすべてが含まれています：

### ✅ フォルダ構造
```
shift_bot_complete/
├── db/                    # データベース関連
│   ├── __init__.py
│   ├── database.py
│   └── migrations/
│       └── 001_initial.sql
├── models/                # データモデル
│   ├── __init__.py
│   ├── user.py
│   ├── shift.py
│   ├── shift_request.py
│   └── shift_revision.py
├── services/              # ビジネスロジック
│   ├── __init__.py
│   ├── shift_optimizer.py
│   ├── shift_approval.py
│   └── line_service.py
├── handlers/              # メッセージ処理
│   ├── __init__.py
│   ├── staff_handler.py
│   └── manager_handler.py
├── utils/                 # ユーティリティ
│   ├── __init__.py
│   ├── labor_law.py
│   ├── validators.py
│   └── audit_log.py
├── monitoring/            # エラー監視
│   ├── __init__.py
│   └── error_handler.py
├── scripts/               # 運用スクリプト
│   ├── __init__.py
│   └── setup_admin.py
├── tests/                 # テスト
│   └── test_basic.py
├── app.py                 # メインアプリ
├── config.py              # 設定
├── requirements.txt       # 必要なライブラリ
├── .env.example           # 環境変数テンプレート
├── .gitignore
├── LICENSE
├── README.md              # 詳細手順書
├── Procfile
└── render.yaml
```

## 📥 ダウンロード後の手順

### 1. ダウンロードしたフォルダの配置

ダウンロードした `shift_bot_complete` フォルダを以下の場所に配置：

```
C:\Users\(あなたのユーザー名)\Documents\
```

### 2. フォルダ名を変更（任意）

`shift_bot_complete` → `shift_bot` に名前を変更してもOK

### 3. PowerShellで開く

1. エクスプローラーで `shift_bot_complete` フォルダを開く
2. アドレスバーをクリック
3. 「powershell」と入力してEnter

### 4. 仮想環境を作成

```powershell
python -m venv venv
venv\Scripts\activate
```

### 5. ライブラリをインストール

```powershell
pip install -r requirements.txt
```

※ psycopg2-binary のエラーは無視してOK（開発環境では不要）

### 6. 環境変数ファイルを作成

```powershell
copy .env.example .env
```

### 7. .env ファイルを編集

メモ帳で `.env` を開いて、以下を設定：
- LINE_CHANNEL_SECRET=（あなたのChannel Secret）
- LINE_CHANNEL_ACCESS_TOKEN=（あなたのChannel Access Token）

### 8. データベースを初期化

```powershell
python -c "from db.database import init_db, create_tables; init_db(); create_tables()"
```

成功すると：
```
✅ All tables created successfully
```

### 9. 完了！

これで準備完了です。次のステップに進めます。

---

## ❓ 問題が発生した場合

- フォルダが見つからない → ダウンロード場所を確認
- エラーが出る → エラーメッセージを確認
- 不明な点 → 気軽に質問してください
